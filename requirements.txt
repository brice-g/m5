# Module 3 — Multi-source, intégration et enrichissement
# Environnement Python recommandé : 3.11
#
# Surcouche au requirements.txt du M2 : on garde l'existant et on ajoute
# ce qu'il faut pour les briefs M3.

# ---------- Repris du M2 ----------
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
ipykernel>=6.29
loguru>=0.7
pytest>=8.0
pytest-cov>=5.0
sqlalchemy>=2.0
psycopg[binary]>=3.1   # PostgreSQL — retirer si MySQL choisi
# pymysql>=1.1         # décommenter si MySQL choisi
alembic>=1.13          # devient obligatoire en M3 (migrations B1, B2, B3)

# ---------- Validation et modèles ----------
pydantic>=2.6

# ---------- Ingestion email (B1) ----------
# 'mailbox' et 'email' sont dans la stdlib — rien à installer
beautifulsoup4>=4.12   # fallback HTML → texte pour les emails multipart
lxml>=5.0              # parser rapide pour bs4
chardet>=5.2           # détection d'encodage des emails legacy

# ---------- Ingestion web et chat (B2) ----------
# JSON et CSV : stdlib + pandas — rien de nouveau

# ---------- Déduplication (B2) ----------
datasketch>=1.6        # MinHash / LSH (bonus B2)

# ---------- NoSQL (bonus B2) ----------
pymongo>=4.6           # source MongoDB optionnelle

# ---------- Enrichissement (B3) ----------
langdetect>=1.0.9      # détection de langue baseline
# fasttext>=0.9.2      # alternative robuste — décommenter si retenu
#                      # nécessite le modèle lid.176.bin à télécharger à part

# Sentiment FR (B3) — optionnel selon l'option choisie
transformers>=4.40
torch>=2.2

# ---------- Embeddings légers (bonus B2 dédup sémantique) ----------
sentence-transformers>=2.7

# ---------- HTTP / API externe ----------
httpx>=0.27
python-dotenv>=1.0
